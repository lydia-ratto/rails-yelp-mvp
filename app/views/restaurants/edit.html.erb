<h1><%= @restaurant.name %></h1>

<%= simple_form_for(@restaurant) do |f| %>
  <%= f.input :name %>
  <%= f.input :phone_number %>
  <%= f.input :address %>
  <%= f.input :category, as: :select, collection: Restaurant::CUISINES, prompt: 'Select category'%>
  <%= f.submit %>
<% end %>
